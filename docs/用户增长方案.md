# Sub2API 用户快速增长方案

> 目标：通过产品优化、邀请机制、渠道推广三大维度，实现用户快速增长

---

## 一、邀请奖励系统

### 1.1 机制设计

#### 核心流程
```
邀请人分享邀请码 → 新用户注册时填写（复用优惠码输入框） → 记录邀请关系 → 新用户首次付费 → 双方各得 $10
```

#### 奖励规则
| 角色 | 奖励内容 | 触发条件 |
|------|---------|---------|
| 邀请人 | $10 额度 | 被邀请人完成首次付费 |
| 被邀请人 | $10 额度 | 自己完成首次付费 |

#### 关键规则
- **复用优惠码**：用户的邀请码填入注册页现有的「优惠码」输入框
- **不限次数**：同一邀请码可被无限次使用
- **延迟生效**：注册时不发放奖励，首次付费后才发放
- **无上限**：邀请奖励无上限
- **自动发放**：首次付费成功后自动发放，无需人工审核（付费即真实用户）

#### 防薅羊毛规则
- **最低付费门槛**：被邀请人首次付费 ≥ ¥29.9 才触发双方奖励（防止 ¥9.9 小号套利）
- **不能邀请自己**：检查邀请人和被邀请人的支付账号是否相同
- **邀请人状态检查**：邀请人被封禁时，仅给被邀请人发放奖励
- **首次付费定义**：指首笔成功且未退款的支付订单（退款后不可再次触发）

### 1.2 技术实现

#### 后端改动

**1. 用户表新增字段**
```sql
ALTER TABLE users ADD COLUMN referral_code VARCHAR(8) UNIQUE;  -- 用户专属邀请码
ALTER TABLE users ADD COLUMN referred_by INT;                   -- 邀请人用户ID
ALTER TABLE users ADD COLUMN referral_rewarded BOOLEAN DEFAULT FALSE;  -- 邀请奖励是否已发放
```

**2. 生成邀请码逻辑**
```python
# 用户注册/首次访问时生成
def generate_referral_code(user_id: int) -> str:
    """
    生成用户专属邀请码
    - 使用前缀 'R' 区分系统优惠码，避免冲突
    - 格式: R + 7位字符 = 8位总长度
    """
    import hashlib
    hash_str = hashlib.md5(f"{user_id}_{secret_key}".encode()).hexdigest()[:7].upper()
    code = f"R{hash_str}"

    # 检查是否与系统优惠码冲突（理论上不会，因为有前缀）
    while promo_code_exists(code):
        hash_str = generate_random_string(7)
        code = f"R{hash_str}"

    return code
```

**3. 优惠码验证逻辑修改**
```python
# /auth/validate-promo-code 接口
def validate_promo_code(code: str) -> dict:
    # 1. 先检查是否是系统优惠码（现有逻辑）
    promo = get_promo_code(code)
    if promo:
        return {"valid": True, "bonus_amount": promo.bonus_amount, "type": "promo"}

    # 2. 再检查是否是用户邀请码
    inviter = get_user_by_referral_code(code)
    if inviter:
        return {
            "valid": True,
            "bonus_amount": 0,  # 注册时不显示奖励金额
            "type": "referral",
            "message": "邀请码有效，首次付费后双方各得 $10"
        }

    return {"valid": False, "error_code": "PROMO_CODE_NOT_FOUND"}
```

**4. 注册逻辑修改**
```python
# /auth/register 接口
def register(email, password, promo_code=None):
    user = create_user(email, password)

    # 生成用户专属邀请码
    user.referral_code = generate_referral_code(user.id)

    if promo_code:
        # 检查是否是邀请码
        inviter = get_user_by_referral_code(promo_code)
        if inviter:
            user.referred_by = inviter.id
            # 注册时不发放奖励，仅记录关系
        else:
            # 系统优惠码，走现有逻辑
            apply_promo_code(user, promo_code)

    return user
```

**5. 首次付费奖励逻辑（自动发放 + 防薅羊毛）**
```python
# 支付成功回调中
def on_payment_success(user_id, order):
    user = get_user(user_id)

    # 检查是否是首次付费 & 有邀请人 & 未发放奖励
    if user.referred_by and not user.referral_rewarded:
        if is_first_payment(user_id):
            inviter = get_user(user.referred_by)

            # ===== 防薅羊毛检查 =====

            # 1. 最低付费门槛：≥ ¥29.9 才触发奖励
            if order.amount < 29.9:
                return  # 不触发邀请奖励，但不影响正常订单

            # 2. 检查支付账号是否相同（防止自己邀请自己）
            if is_same_payment_account(user_id, user.referred_by):
                log_suspicious_referral(user_id, user.referred_by, "相同支付账号")
                return

            # 3. 检查邀请人状态
            inviter_reward = 10.0
            if inviter.status == 'banned':
                inviter_reward = 0  # 邀请人被封禁，不发放其奖励

            # ===== 发放奖励 =====

            if inviter_reward > 0:
                add_balance(user.referred_by, inviter_reward)  # 邀请人 +$10
                inviter.referral_count += 1

            add_balance(user_id, 10.0)  # 被邀请人 +$10

            # 标记已发放
            user.referral_rewarded = True

            # 记录日志
            log_referral_reward(
                inviter_id=user.referred_by,
                invitee_id=user_id,
                inviter_reward=inviter_reward,
                invitee_reward=10.0
            )

def is_same_payment_account(user_id_a, user_id_b):
    """检查两个用户是否使用相同的支付账号"""
    # 获取两个用户的支付记录中的支付账号（支付宝/微信 openid 等）
    accounts_a = get_user_payment_accounts(user_id_a)
    accounts_b = get_user_payment_accounts(user_id_b)
    return bool(accounts_a & accounts_b)  # 有交集则视为同一人
```

**6. 邀请奖励记录表（用于统计和展示）**
```sql
CREATE TABLE referral_rewards (
    id INT PRIMARY KEY AUTO_INCREMENT,
    inviter_id INT NOT NULL,
    invitee_id INT NOT NULL,
    reward_amount DECIMAL(10,2) DEFAULT 10.00,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_inviter (inviter_id),
    INDEX idx_invitee (invitee_id)
);
```

#### 前端改动

**1. 注册页面（几乎无改动）**
- 现有优惠码输入框已支持
- 仅需调整验证成功后的提示文案：
  - 系统优惠码：「优惠码有效，注册后获得 $X」
  - 邀请码：「邀请码有效，首次付费后双方各得 $10」

**2. 用户中心新增「邀请好友」页面**
```
路由: /user/referral 或 /dashboard/referral

页面内容:
┌─────────────────────────────────────────────────────┐
│  邀请好友                                            │
├─────────────────────────────────────────────────────┤
│                                                      │
│  你的专属邀请码                                       │
│  ┌──────────────────────────────────┐               │
│  │  ABC12345                    [复制]│              │
│  └──────────────────────────────────┘               │
│                                                      │
│  邀请链接                                            │
│  ┌──────────────────────────────────┐               │
│  │  https://xxx.com/register?promo=ABC12345  [复制] │ │
│  └──────────────────────────────────┘               │
│                                                      │
│  ┌─────────────────────────────────────────────────┐│
│  │  📋 邀请规则                                     ││
│  │  1. 好友通过你的邀请码注册                        ││
│  │  2. 好友完成首次付费后，双方自动各得 $10          ││
│  │  3. 邀请奖励无上限                               ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  邀请记录                                            │
│  ┌─────────────────────────────────────────────────┐│
│  │ 用户        │ 注册时间    │ 状态      │ 奖励     ││
│  │ u***@xx.com │ 2025-01-20 │ 待付费    │ -        ││
│  │ a***@xx.com │ 2025-01-15 │ 已发放    │ $10      ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  累计邀请: 2 人  │  已付费: 1 人  │  累计奖励: $10   │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 1.3 改动量评估

| 模块 | 改动内容 | 工作量 |
|-----|---------|-------|
| 后端 - 数据库 | 用户表加 3 字段，新增奖励记录表 | 小 |
| 后端 - API | 修改验证接口，新增邀请相关接口 | 中 |
| 后端 - 支付 | 首付自动发放奖励逻辑 | 小 |
| 前端 - 注册页 | 调整提示文案 | 极小 |
| 前端 - 用户中心 | 新增邀请页面 | 中 |

**总体评估**：改动较小，核心是复用现有优惠码体系，付费后自动发放无需审核

---

## 二、套餐体系

### 2.1 完整套餐列表

| 套餐 | 价格 | 月额度 | 单价 | 定位 |
|-----|------|-------|-----|-----|
| 体验版 | ¥9.9 | $22 | ¥0.45/刀 | 新人体验 |
| 基础版 | ¥29.9 | $70 | ¥0.43/刀 | 轻度使用 |
| **入门版** | **¥49.9** | **$120** | **¥0.42/刀** | **日常开发** |
| 标准版 | ¥69.9 | $170 | ¥0.41/刀 | 主力推荐 |
| **进阶版** | **¥99.9** | **$250** | **¥0.40/刀** | **中度使用** |
| 专业版 | ¥149.9 | $380 | ¥0.39/刀 | 重度使用 |

### 2.2 按量计费（可选）

- **起充金额**：¥10
- **单价**：¥0.50/刀
- **特点**：无有效期，用完为止

---

## 三、渠道推广策略

### 3.1 开发者社区

| 平台 | 内容形式 | 频率 |
|-----|---------|-----|
| V2EX | 技术分享帖 | 每周 1 篇 |
| 掘金 | 教程文章 | 每周 2 篇 |
| 知乎 | 问答 + 文章 | 每周 2-3 次 |
| 即刻 | 短内容 | 每日 1-2 条 |

### 3.2 内容选题

1. 《Claude Code 国内使用完整指南》
2. 《5 分钟配置本地 AI 编程助手》
3. 《Claude vs GPT：编程场景实测对比》
4. 《Cursor + Claude API 最佳实践》

### 3.3 SEO 目标关键词

- Claude API 国内
- Claude Code 代理
- Anthropic API 镜像

---

## 四、实施优先级

### P0 - 立即执行（1-2 周）

| 序号 | 任务 | 说明 |
|-----|-----|-----|
| 1 | 邀请码系统 | 用户专属邀请码 + 邀请记录 |
| 2 | 新增套餐 | ¥49.9 和 ¥99.9 |
| 3 | 用户中心邀请页 | 展示邀请码和记录 |

### P1 - 短期执行（3-4 周）

| 序号 | 任务 |
|-----|-----|
| 6 | 内容营销启动 |
| 7 | 状态页面完善 |
| 8 | 管理后台邀请统计（可选） |

---

## 五、数据指标

| 指标 | 目标 |
|-----|-----|
| 日注册用户 | 50+ |
| 付费转化率 | ≥12% |
| 邀请转化率 | ≥5% |
| 次月留存 | ≥40% |

---

*文档版本：v1.2*
*更新日期：2025-01-25*
