# 模型定价升级方案(全量表可见 · 一键拉价 · 阶梯价取舍)

> 状态(2026-06-22):**P0 全量表可见 + P1 一键拉价(diff 预览)已实现并自测通过**;P2 阶梯价/缓存读写分价按本方案缓做。下方为完整分析与方案,末尾附「本次落地」清单。

---

## 实施状态速览

| 阶段 | 内容 | 状态 | 落地文件 |
|---|---|---|---|
| P0 | 让全量表(Claude/GPT/Gemini/国产)在「模型定价」页只读可见 + 「覆盖此项」 | ✅ 已实现 | `pricing_builtin_view.go`(NEW)、`model_pricing_handler.go`、`ModelPricingView.vue`、`modelPricing.ts` |
| P1 | 一键拉取最新价 + diff 预览(preview/apply 两步) | ✅ 已实现 | 同上 + `admin.go` 路由、`pricing_builtin_view.go` 的 `PreviewRemotePricing` |
| P2 | 阶梯/分层价、缓存读写分价 | ⏸ 缓做(见 §4) | — |

**后端新增接口**:`GET /admin/model-pricing` 现返回 `{entries, builtin}`;`POST /admin/model-pricing/refresh/preview`(diff,不落库)、`POST /admin/model-pricing/refresh/apply`(`ForceUpdate` 落库)。
**自测**:`go build ./...` 通过;`ListBuiltinPricing` / `PreviewRemotePricing` 单测通过;前端 `vue-tsc` 通过。

---

## 模型定价子系统整合方案:让全量表可见 + 一键拉价 + 阶梯价取舍

### 1. 结论先行

- **(Q1) 站长的直觉只对了一半。** 「模型定价」页 **不是全系统价格表,而是一张「覆盖表(override table)」**——它只列管理员手填的项,数据源唯一是 settings 表的 `model_pricing_overrides` key(`pricing_overrides.go:69`),**完全不读** Claude/GPT 所在的内置全量表 `model_pricing.json`。所以 Claude/GPT 默认不显示是 by design,不是 bug。真正的「全量有效价」分散在三层:UI 覆盖(最高优先)→ 国产 ¥ 硬编码表 → LiteLLM JSON 全量表(`pricing_service.go:767-849`)。要让它变成「全量有效价表」,**改 List 接口合并返回内置表只读视图 + 覆盖可编辑**即可,无需动计费热路径。
- **(Q2) 阶梯价我们目前几乎用不上,建议 P2 再做、先用前缀覆盖兜底。** new-api 有两套阶梯方案(同名异档 + `tiered_expr` 表达式计费),但触发场景(Gemini >200k 翻倍、缓存读写分价)在我们这边要么已被 LiteLLM 表的 `cache_read/cache_creation` 字段覆盖,要么可以用「`gemini-2.5-pro` 整模型一档 + 必要时拆 key」兜住,ROI 偏低。
- **(Q3) 一键拉价我们后端能力已经齐了,只差一个路由 + handler。** `PricingService.ForceUpdate()`(`pricing_service.go:1237`)已实现「拉远程 LiteLLM 镜像 JSON → 解析 → 落盘 → 刷内存」全流程且线程安全,但 **grep 全仓没有任何 HTTP 路由挂它**。推荐方案 = 复用 ForceUpdate + 加 diff 预览,数据源用 LiteLLM `model_prices_and_context_window.json`(绝对 USD/token,与我们口径天然一致,不像 new-api 倍率制要乘 0.002 反算)。
- **关键认知纠偏:** 「`model_pricing.json` 不可手改」的真因 **不是 sha256 校验拦截**,而是每 10 分钟 `syncWithRemote` 发现 hash 不一致就整文件覆盖(`pricing_service.go:373`)。一键拉取走的正是这条同步链路,**天然不冲突、不会被回滚**。

---

### 2. 现状画像

#### 2.1 我们的定价三层(计费热路径,`GetModelPricing` `pricing_service.go:767-849`)

```
请求模型名 modelLower
   │
   ├─① UI 覆盖表  matchOverride(modelLower)          ← settings.model_pricing_overrides
   │     命中即 return overrideToLiteLLM(ov)            优先级最高,在 s.mu.RLock 之前
   │     (pricing_service.go:776-781)
   │
   │  s.mu.RLock() ↓
   ├─② 国产 ¥ 硬编码表  kimi/deepseek/qwen            ← .go 源码内常量表,改要改代码
   │     (pricing_service.go:788-800)                   按 pricing.cny_to_usd_rate 折算
   │
   └─③ LiteLLM 全量 JSON 表  pricingData               ← data/model_pricing.json(运行时缓存)
         精确→变体→去日期模糊→Claude家族→OpenAI回退      Claude/GPT/Gemini 真实价在这里
         (pricing_service.go:802-849)
```

**三层数据源与可改性对照:**

| 层 | 数据源 | 落地位置 | 怎么改 | 是否在「模型定价」页 |
|---|---|---|---|---|
| ① UI 覆盖 | settings 表 `model_pricing_overrides` | DB | UI 直接编辑(本页) | ✅ 是(唯一来源) |
| ② 国产 ¥ | `kimiMoonshotPricingTable`/`deepSeekPricingTable`/`qwenPricingTable` | `.go` 源码 | 改代码重新部署 | ❌ 否 |
| ③ LiteLLM 全量 | `data/model_pricing.json`(1.29MB / 200 条) | 运行时缓存文件 | 远程同步刷新,不可手改 | ❌ 否 |

**覆盖表单条记录形状**(`modelPricingOverride` `pricing_overrides.go:19-27` / 前端 `modelPricing.ts:8-16`),单位 **每百万 token**:

```json
{ "model":"claude-3-5-sonnet", "currency":"CNY|USD",
  "input":3.0, "output":15.0, "cache":0.3,
  "has_cache":true, "enabled":true }
```

**币种口径**(`overrideToLiteLLM` `pricing_overrides.go:165-179`):内部价一律折成「每 token 美元」,`CNY` 除以运行时汇率 `cnyToUSDRate()`,`USD` 不除。**匹配规则**(`matchOverride` `pricing_overrides.go:126-161`):先精确(完整名 + 去 provider 前缀的 lastSegment)、无则按 model 长度降序的**最长前缀回退**。

#### 2.2 内置全量表来源与同步(`pricing_service.go`)

- 文件路径 = `getPricingFilePath()` = `filepath.Join(cfg.Pricing.DataDir, "model_pricing.json")`(`:1242`),`data_dir` 默认 `./data`(`config.go:1820`),进程 CWD 在 backend 下时落到 `backend/data/model_pricing.json`。
- 远程源 `pricing.remote_url` 默认 `https://raw.githubusercontent.com/Wei-Shaw/model-price-repo/main/model_prices_and_context_window.json`(LiteLLM 镜像),`hash_url` 同目录 `.sha256`(`config.go:1818-1819`);HTTP 在 `repository/pricing_service.go:58 FetchPricingJSON`。
- 调度:`startUpdateScheduler()`(`:205`)每 `hash_check_interval_minutes`(默认 10 分钟)`syncWithRemote()`,hash 变就重下;启动时 `checkAndUpdatePricing()`(`:234`);兜底 `useFallbackPricing()`(`:514`)复制 `backend/resources/model-pricing/model_prices_and_context_window.json`(192 条,Last updated 2025-08-10)。
- **sha256 仅作同步锚点**:`downloadPricingData`(`:358-363`)hash 不一致只告警不阻断;`loadPricingData`(`:494`)算本地 hash 仅用于比对决定是否重下。

#### 2.3 现有「获取」能力(均为只读内置表,不联网)

| 接口 | 路由 | 真实行为 |
|---|---|---|
| `GetModelDefaultPricing` | `GET /admin/channels/model-pricing` | 查内存 `pricingData`+覆盖表,返回单价给 UI 自动填充。纯读 |
| `SyncPricingModels` | `GET /admin/channels/pricing/sync-models?platform=` | 名字带 Sync 但**不联网**,只 `ListModelNamesByProvider` 列内存模型名 |
| `ForceUpdate`/`ForceUpdatePricing` | **无路由** | 真正的联网拉取全流程已实现,但 grep 全仓无路由调用 |

#### 2.4 new-api 倍率体系对照(我们=绝对单价,new-api=倍率)

| 维度 | 我们 sub2api | new-api |
|---|---|---|
| 计价基准 | **绝对单价 / 每百万 token** | **倍率** `ratio 1 = $0.002/1K = $2/百万 token` |
| 输出价 | 独立 `output` 绝对值 | `completion_ratio`(相对输入倍数) |
| 缓存价 | 单个 `cache`(读),`has_cache` 开关 | `cache_ratio` + `create_cache_ratio` 读写分离 |
| 按次计价 | 无 | `model_price`(dall-e-3 等绝对单价) |
| 分组差异 | 无 | `group_ratio` `{default/vip/svip}` + 嵌套 |
| 阶梯价 | 无 | 同名异档 + `tiered_expr` 表达式(`billing_mode`/`billing_expr`) |
| 默认表来源 | 远程 LiteLLM 镜像(可拉) | 硬编码常量 map(`defaultModelRatio`) |
| 一键同步 | ForceUpdate 已实现未挂路由 | `POST /api/ratio_sync/fetch`(多上游 + diff) |
| 展示接口 | 覆盖表 List(不全) | `GET /api/pricing`(全量 + 分组过滤) |

---

### 3. Q1:让全量表可见

**目标**:把「模型定价」页从「覆盖表」升级为「全量有效价表」——内置 ② ③ 层只读展示,① 层覆盖可编辑,前端区分「默认值 / 已覆盖」。**不动计费热路径**,只改 List 读侧。

#### 3.1 最小改动方案(P0,推荐先落)

只让 List 接口在返回覆盖项之外,**合并返回内置全量表的只读快照**。

**改 3 个文件:**

1. **`pricing_service.go` 暴露内置表只读视图**(新增方法,不动 `GetModelPricing`):
   ```go
   // 在 s.mu.RLock() 下遍历 pricingData,折算回「每百万 token」+ 标 currency=USD
   func (s *PricingService) ListBuiltinPricing() []BuiltinPricingEntry
   ```
   每条返回 `{model, input_per_m, output_per_m, cache_read_per_m, cache_write_per_m, source:"litellm"|"cny", currency}`。国产 ¥ 表(②)也一并 dump 进来标 `source:"cny"`、`currency:"CNY"`,这样 Claude/GPT/Gemini/qwen 全部可见。注意:**遍历必须在 RLock 内完成构造切片后立即解锁**,不在锁内做慢操作(沿用热路径约束)。

2. **`model_pricing_handler.go` List 合并**(`:19-26`):返回结构升级为
   ```json
   { "overrides":[...], "builtin":[...] }
   ```
   `overrides` 仍是可编辑的 settings 覆盖项;`builtin` 是只读快照。前端按 `model` 做 left-join。

3. **`ModelPricingView.vue` 渲染区分**:
   - 表格新增「来源」列:`已覆盖(override)` / `默认(litellm)` / `默认(国产¥)`。
   - `loadConfig`(`:107-117`)把 `builtin` 与 `overrides` 合并:同 model 名时覆盖项盖默认项并标记 `overridden=true`,默认项灰显 + 提供「覆盖此项」按钮(点了等于 `addRow` 预填默认值)。
   - 默认行的输入框 `:disabled`,只有「覆盖」后才可编辑。

**验收点**:进页面即可看到 Claude/GPT/Gemini/qwen 全量默认价;手填一行 claude 覆盖后,该行标「已覆盖」且计费生效(覆盖优先级不变)。

#### 3.2 完整方案(可选增强)

- 默认表支持搜索/分页(200 条);
- 「来源」可点开 tooltip 显示命中的是精确匹配还是家族回退(复用 `matchByModelFamily` 的判定);
- 覆盖项支持「重置为默认」(删除该 entry,回落内置表);
- 展示 `cny_to_usd_rate` 当前汇率,CNY/USD 双栏对照。

---

### 4. Q2:阶梯/分层价

#### 4.1 我们是否真需要?——逐场景判断

| 真实场景 | 我们现状能否兜住 | 结论 |
|---|---|---|
| Gemini >200k 输入翻倍 | LiteLLM 表对 gemini-2.5-pro 默认给的是 ≤200k 档价;>200k 我们当前**会少计费** | 唯一真缺口,但量小 |
| 各厂长上下文档位 | 多数厂商把长上下文做成**独立模型名**(如 `-200k` 后缀),前缀覆盖即可分价 | 不需阶梯,前缀兜底 |
| 缓存读 / 写分价 | LiteLLM 表已有 `cache_read_input_token_cost` + `cache_creation_input_token_cost` 两字段;计费已分别取用 | 已支持,只是覆盖表 UI 只暴露了单个 `cache`(读) |
| batch 半价 | 我们无 batch 端点 | 不适用 |

**判定:阶梯价 ROI 低,P2 再做。** 唯一真缺口是 Gemini >200k,可先用「为 `gemini-2.5-pro` 整模型设一档保守价 + 必要时按渠道拆 key」兜底,不值得为它引入表达式引擎。

#### 4.2 new-api 的做法(对标)

- **同名异档**:`yi-34b-chat-200k` 作独立 key,`GetCompletionRatio` 对 Gemini 各代按模型名写死不同倍率——本质是「把档位编进模型名」,我们的前缀覆盖等价可做。
- **`tiered_expr` 表达式计费**(`model.Pricing.billing_mode/billing_expr`):`tokens <= 128000 ? 0.002 : 0.004`、`p < 200000 ? tier("480p", p*7.877) : tier("1080p", p*8.733)`。变量 `p/c/tokens`、函数 `tier()`、三元运算。**已知坑(Issue #4523)**:设 `tiered_expr` 会清空 `ModelRatio`,旧校验只看 ModelRatio 误判未定价 → `model_price_error`。我们若做要避开同类校验耦合。

#### 4.3 若将来要做——数据模型扩展建议(P2)

给 `modelPricingOverride` 增可选 `tiers` 字段(不破坏现有结构,旧记录无此字段走原逻辑):

```json
{ "model":"gemini-2.5-pro", "currency":"USD", "enabled":true,
  "input":1.25, "output":10.0,
  "tiers":[
    { "metric":"input_tokens", "gt":200000, "input":2.5, "output":15.0 }
  ] }
```

- **匹配逻辑改动**:`overrideToLiteLLM`(`pricing_overrides.go:165`)目前返回固定单价;改为返回一个「能按请求 token 数选档」的结构,由计费侧在已知 token 数后选档。**注意**:这要求定价不再是「请求前一次性查表」,而是「拿到 usage 后再定档」,会触及 `GetModelPricing` 调用时序,改动面比 Q1/Q3 大得多——这也是建议押后的原因。
- **不引入表达式引擎**:相比 new-api 的 `tiered_expr`,我们用「阈值数组」声明式表达足够覆盖 Gemini >200k 这类需求,避免引入 expr 解析器和它的校验耦合坑。

---

### 5. Q3:一键获取最新价格

#### 5.1 三方案打分对比

| 方案 | 数据源 | 字段映射 | 币种 | 与 sha256 冲突 | 工作量 | 评分 |
|---|---|---|---|---|---|---|
| **A. LiteLLM 拉取刷新内置表** | `model_prices_and_context_window.json`(绝对 USD/token) | 直取 4 字段 ×1e6 | 全 USD,口径一致 | **无冲突**(走同步链路) | 极小(复用 ForceUpdate) | ⭐⭐⭐⭐⭐ |
| **B. new-api / OpenRouter 价表导入** | `/api/pricing`(倍率)/ OpenRouter `/v1/models`(USD字符串) | 倍率需 ×0.002 反算 / OpenRouter 直取 | 倍率耦合分组,需反推 | 同上 | 中(格式各异 + 别名) | ⭐⭐⭐(OpenRouter 可做补充源) |
| **C. 渠道 /v1/models 拉模型名 + 手填价** | 渠道 `/v1/models` | 只有模型名,无价 | 全手填 | 不涉及 | 大(纯人工) | ⭐⭐ |

#### 5.2 推荐:方案 A(主)+ OpenRouter 补充(后续可选)

**数据源**:LiteLLM `model_prices_and_context_window.json`(我们 `remote_url` 默认就指向它的镜像 `Wei-Shaw/model-price-repo`)。

**字段映射(LiteLLM → 我们每百万 token):**
```
input_per_m       = input_cost_per_token           × 1_000_000
output_per_m      = output_cost_per_token          × 1_000_000
cache_read_per_m  = cache_read_input_token_cost    × 1_000_000
cache_write_per_m = cache_creation_input_token_cost× 1_000_000
```
键名需别名归一(bedrock/vertex 前缀变体 → 内部模型名)。

**币种处理**:LiteLLM 全 USD,直接落内置表(③ 层本就是 USD 口径)。**国产模型不动**——qwen/glm/deepseek 的官方 ¥ 价继续走 ② 层 .go 硬编码 + ① 层 UI override,LiteLLM 对国产覆盖差,作兜底即可。这与现状一致,不会冲掉手填的国产价。

**与 sha256 防改缓存的冲突——不存在**:ForceUpdate 直接写 `data/model_pricing.json` 并把 `localHash` 设为远程 hash,与 10 分钟定时器逻辑天然一致,**随后的 syncWithRemote 不会回滚**(`pricing_service.go:1237` → `downloadPricingData`)。所谓「绕过 sha256」是伪命题。

**放哪个 handler / 是否带 diff 预览**:

- **后端 service 无需新增**,复用 `PricingService.ForceUpdate()`(`:1237`)/ `BillingService.ForceUpdatePricing()`(`billing_service.go:1161`)。
- **handler**:`model_pricing_handler.go`(已持 `*service.PricingService`,`NewModelPricingHandler:14`)加 `ForceSync(c)`,顺带返回 `GetStatus()`(`:1225`,给 `model_count/last_updated/local_hash`)。
- **路由**:`admin.go:182` 的 `/model-pricing` 组下加 `g.POST("/refresh", …)`,与 List/Update 同组。
- **diff 预览(强烈建议)**:不要一键直接落库。分两步——
  1. `POST /model-pricing/refresh/preview`:`FetchPricingJSON` 拉远程到内存,与当前 `pricingData` 做 diff,返回 `{added, removed, changed:[{model, old:{...}, new:{...}}]}`,**不落盘**;
  2. `POST /model-pricing/refresh/apply`:管理员确认后才 ForceUpdate 落盘刷内存。
- **注意白名单**:`remote_url` 若改指活跃上游(如 LiteLLM 官方仓),需过 `cfg.Security.URLAllowlist.PricingHosts`(`validatePricingURL:554`)。当前默认镜像 `Wei-Shaw/model-price-repo` 若停更,需把 remote_url 指到活跃源或自建镜像。

---

### 6. 分阶段实施清单

#### P0 — 让全量表可见(Q1,最高优先,无热路径风险)
- **改文件**:
  - `pricing_service.go` 新增 `ListBuiltinPricing()`(RLock 内构造切片即解锁)
  - `model_pricing_handler.go:19-26` List 返回 `{overrides, builtin}`
  - `ModelPricingView.vue:107-117` 合并渲染 + 来源列 + 「覆盖此项」按钮
  - `modelPricing.ts` 类型补 `builtin` 字段
- **验收**:进页面即见 Claude/GPT/Gemini/qwen 全量默认价;覆盖一行后标「已覆盖」且计费优先级不变;空覆盖表不再是空白页。

#### P1 — 一键拉取 + diff 预览(Q3)
- **改文件**:
  - `model_pricing_handler.go` 加 `ForceSync` / `RefreshPreview` / `RefreshApply`
  - `admin.go:182` 路由组加 `POST /model-pricing/refresh/preview` + `/apply`
  - 复用 `pricing_service.go:1237 ForceUpdate` + `repository/pricing_service.go:58 FetchPricingJSON`
  - `ModelPricingView.vue` 加「拉取最新价」按钮 + diff 弹窗
  - 确认 `cfg.Security.URLAllowlist.PricingHosts` 含目标源;核实 `remote_url` 镜像仍在更新
- **验收**:点「拉取」出 diff 预览(added/removed/changed),确认后落 `data/model_pricing.json` 且 10 分钟同步不回滚;`GetStatus` 显示 `last_updated` 刷新。

#### P2 — 阶梯价 / 缓存读写分价(Q2,ROI 低,可缓)
- **改文件**:
  - `pricing_overrides.go` 给 `modelPricingOverride` 加可选 `tiers` + 覆盖表 UI 暴露 `cache_write`(读写分价)
  - `GetModelPricing` 调用时序改造以支持「按 usage token 选档」(改动面最大,需单独评审)
  - `ModelPricingView.vue` 阶梯编辑 UI
- **验收**:为 gemini-2.5-pro 配 >200k 档,实测大 context 请求按高档计费;缓存读写分别计价正确。

---

### 7. 风险与易错点(沿用本仓既有约束)

1. **`model_pricing.json` 是运行时同步缓存,不可手改**——真因是每 10 分钟 `syncWithRemote` 整文件覆盖(`pricing_service.go:373`),**不是 sha256 拒绝**。任何「更新内置表」必须走 ForceUpdate / 同步链路,不要直接写文件,否则下次同步被冲掉。
2. **币种口径 1¥ = 1 余额单位**,国产模型按 ¥ 展示。一键拉取 LiteLLM 只刷 USD 内置表,**绝不能用 LiteLLM 的国产模型价冲掉 ② 层官方 ¥ 价**;国产价仍以 .go 硬编码 + UI override 为准。
3. **matchOverride 最长前缀**(`pricing_overrides.go:152-159`,list 按 model 长度降序)——Q1 让默认表可见后,前端 left-join 要用同一套「精确 + 最长前缀」语义判断「该默认项是否已被某覆盖项盖住」,否则 UI 标记的「已覆盖」会与实际计费命中不一致。
4. **计费热路径不能在 RLock 内拉慢 DB / 做慢操作**——`ListBuiltinPricing` 只在锁内构造切片后立即解锁,diff/网络拉取一律在锁外;覆盖表读侧已有 60s TTL + singleflight(`pricing_overrides.go:66-90`),P0 的 List 合并不要破坏它。
5. **两份 model_pricing.json 内容/哈希不同**(根 `data/` vs `backend/data/`,sha256 各异)——真正被加载的是进程 CWD 下 `./data/model_pricing.json`。落地一键拉取前先确认部署环境的实际 CWD 与 `data_dir`,避免刷错文件。
6. **国产 ¥ 价改不了走一键**——②层是 .go 硬编码,一键拉取只动 ③层 JSON。要改国产价只能改代码或用 UI override,文档需向站长讲清这条边界。
7. **diff 预览务必默认开启**——LiteLLM 上游一次更新可能改动几十个模型价,直接落库无审阅会让线上计费突变;`refresh/preview` 与 `refresh/apply` 分离是硬要求。

**核心改动文件清单(绝对路径):**
- `/Users/Apple/Project/sub2api/backend/internal/service/pricing_service.go`
- `/Users/Apple/Project/sub2api/backend/internal/service/pricing_overrides.go`
- `/Users/Apple/Project/sub2api/backend/internal/service/billing_service.go`
- `/Users/Apple/Project/sub2api/backend/internal/handler/admin/model_pricing_handler.go`
- `/Users/Apple/Project/sub2api/backend/internal/repository/pricing_service.go`
- `/Users/Apple/Project/sub2api/backend/internal/server/routes/admin.go`
- `/Users/Apple/Project/sub2api/frontend/src/views/admin/ModelPricingView.vue`
- `/Users/Apple/Project/sub2api/frontend/src/api/admin/modelPricing.ts`